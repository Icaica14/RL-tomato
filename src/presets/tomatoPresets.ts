import { ActionId, ActionEffect, TomatoCell, TomatoConfig } from '../types/tomato';
import { bucketLabels } from '../rl/environment';
export const defaultActions:Record<ActionId,ActionEffect>={do_nothing:{id:'do_nothing',label:'Do Nothing',badge:'N',actionCost:0,waterDelta:-10,nutrientDelta:-5,explanation:'The plant consumes water and nutrients naturally.'},water:{id:'water',label:'Water',badge:'W',actionCost:-1,waterDelta:25,nutrientDelta:-5,explanation:'You add water. This can help dry plants but can damage the plant if water becomes excessive.'},fertilize:{id:'fertilize',label:'Fertilize',badge:'F',actionCost:-2,waterDelta:-5,nutrientDelta:25,explanation:'You add nutrients. This can support fruit growth but can damage the plant if nutrients become excessive.'}};
function makeCells(overwater=0,nutrientBurn=0,sparse=false):TomatoCell[]{const cells:TomatoCell[]=[]; for(let y=0;y<5;y++) for(let x=0;x<5;x++){let type:TomatoCell['cellType']='Recovery', reward=0,h=0,f=0,label='Recovery zone', exp='This region is survivable but not ideal.'; const danger=x===0||x===4||y===0||y===4; if(x===2&&y===2){type='Balanced growth';reward=sparse?0:4;h=1;f=3;label='Balanced growth';exp='Water and nutrients are balanced, so the plant can build fruit.'} else if(x===0&&y===0){type='Double stress';reward=sparse?0:-8;h=-8;label='Double stress';exp='Both resources are critically low.'} else if(x<=1){type='Dry stress';reward=sparse?0:-3;h=-2;label='Dry stress';exp='The plant lacks water.'} else if(x===4){type='Overwatered';reward=sparse?0:-4-overwater;h=-3-overwater;label='Overwatered';exp='Too much water stresses roots.'} else if(y<=1){type='Nutrient deficiency';reward=sparse?0:-3;h=-2;label='Nutrient deficiency';exp='The plant lacks nutrients.'} else if(y===4){type='Nutrient excess';reward=sparse?0:-4-nutrientBurn;h=-3-nutrientBurn;label='Nutrient excess';exp='Too many nutrients burn the plant.'} if(danger&&x!==2&&y!==2) f=0; cells.push({id:`cell-${x}-${y}`,x,y,waterBucketLabel:bucketLabels[x],nutrientBucketLabel:bucketLabels[y],cellType:type,rewardOnEnter:reward,healthDelta:h,fruitDelta:f,terminal:false,label,explanation:exp});} return cells;}
export function balancedTomato():TomatoConfig{return {gridSize:5,harvestDay:30,rewardMode:'shaped',cells:makeCells(),actions:defaultActions,rewardConstants:{deathPenalty:-100,harvestFruitWeight:1,harvestHealthWeight:.3,stressPenaltyMultiplier:.2,fruitGainMultiplier:1,healthGainMultiplier:.1},seed:7};}
export const presets=[{name:'Balanced Tomato',config:balancedTomato()},{name:'Sparse Harvest Reward',config:{...balancedTomato(),rewardMode:'sparse' as const,cells:makeCells(0,0,true)}},{name:'Overwatering Trap',config:{...balancedTomato(),cells:makeCells(5,0)}},{name:'Nutrient Burn',config:{...balancedTomato(),cells:makeCells(0,5)}},{name:'Minimal TD Demo',config:{...balancedTomato(),harvestDay:8}}];
